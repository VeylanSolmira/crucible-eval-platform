# Example environment variables for Crucible Platform

# Docker socket location (platform-specific)
# macOS with Docker Desktop:
DOCKER_SOCKET_PATH=/Users/$USER/.docker/run/docker.sock

# Linux:
# DOCKER_SOCKET_PATH=/var/run/docker.sock

# Other configuration
LOG_LEVEL=INFO
BIND_HOST=0.0.0.0
SERVICE_HOST=crucible-platform
PLATFORM_HOST=crucible-platform