apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
# The handler must match the runtime handler name configured in containerd
handler: runsc
# No scheduling constraints needed - gVisor is installed on all nodes via DaemonSet