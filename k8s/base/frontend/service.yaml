apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend-deployment
spec:
  type: LoadBalancer  # In kind, this will give us localhost access
  selector:
    app: frontend-deployment
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP