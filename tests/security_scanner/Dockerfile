# Security Scanner Service - PLACEHOLDER
# This Dockerfile is a placeholder for future microservice deployment

FROM python:3.9-slim

WORKDIR /app

# Copy the scanner code
COPY . .

# Future: Install dependencies for standalone service
# RUN pip install -r requirements.txt

# Future: Run as FastAPI service (main.py uses FastAPI)
# CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8006"]

# Current: Security scanner is run via platform
# To run security tests directly from platform:
# cd src/platform
# python extreme_mvp_frontier_events.py --security-demo
CMD ["echo", "Security Scanner - use via platform with --security-demo or --security-test flags"]